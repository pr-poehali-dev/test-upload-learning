
importя
    navigate("/teacher");
  };

  return ( { useState, useEffect } from 'react';
    <Layout>
      <div className="max-w-3xl mx-auto space
import { Link } from 'react-router-dom';-y-6">
        <div>
          <h
import { Test } from '@/types';
import { tests } from '@/data/1 className="text-3xl font-bold tracking-tight">Создание нового теста</hmockData';
import Layout from '@/components/1>
          <p className="text-Layout';
import { Button } from '@/muted-foreground">
            Заcomponents/ui/button';
import { Card, CardContent, CardHeader, CardTitle,полните форму для создания нового теста
          </p>
        </div>
        
        <TestForm on CardFooter } from '@/components/ui/carSubmit={handleSubmit} />
      d';
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@</div>
    </Layout>
  );
};

export default CreateTest;
